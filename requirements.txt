# Runtime dependencies. Pinned to minimum working versions rather than exact
# pins: this is a library and a CLI, not a deployed service, and over-pinning a
# research tool makes it unusable inside somebody else's environment.
cvxpy>=1.5
numpy>=1.26
pandas>=2.1
scipy>=1.11
matplotlib>=3.8
rich>=13.7
